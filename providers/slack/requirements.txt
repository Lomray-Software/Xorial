# Slack provider runtime dependencies.
# Keep the list minimal — heavy lifting is delegated to libraries,
# only local glue lives in this package.

slack_bolt>=1.28.0        # Slack bot framework (AsyncApp + Socket Mode)
aiohttp>=3.9.0            # Required by slack_bolt's AsyncApp; slack_bolt 1.28 dropped the [async] extra
claude-agent-sdk>=0.1.63  # Official Anthropic SDK wrapping Claude Code CLI (needs Python 3.10+)
httpx>=0.27.0             # Slack file downloads
