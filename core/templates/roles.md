```button
name ← Project
type link
action obsidian://open?vault=context&file=project-context
color default
```

# Custom Roles

> **Inherits from Xorial core** — base roles (`orchestrator`, `implementer`, `reviewer`, etc.) live in `xorial_path/core/roles/`. See `.xorial/config.json → xorial_path`.
> Files in this folder override or extend those roles for this project only.

Project-specific role overrides.
